version: '3.8'

services:
  banana-api:
    build: .
    container_name: banana_ripeness_app
    ports:
      - "5000:5000"
    volumes:
      - ./model:/app/model
      - ./frontend:/app/frontend
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
